### 2\. 리스트의 합 (Sum of List)

**문제:** 숫자로 이루어진 리스트 `nums`를 입력받아, 리스트의 모든 요소의 합을 반환하는 재귀 함수 `list_sum(nums)`을 작성하세요.

**시작 코드:**

```python
def list_sum(nums):
  # 여기에 코드를 작성하세요
  pass

# 테스트
print(f"[1, 2, 3, 4, 5]의 합 = {list_sum([1, 2, 3, 4, 5])}") # 예상 출력: 15
print(f"[]의 합 = {list_sum([])}") # 예상 출력: 0
```