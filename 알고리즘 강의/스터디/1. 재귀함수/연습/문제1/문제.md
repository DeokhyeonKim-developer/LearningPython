## 재귀 함수 파이썬 연습 문제

재귀 함수 알고리즘을 연습하기 좋은 기초 문제 3가지입니다.

-----

### 1\. 팩토리얼 (Factorial)

**문제:** 0 또는 양의 정수 `n`을 입력받아, `n!` (n 팩토리얼) 값을 반환하는 재귀 함수 `factorial(n)`을 작성하세요.

  * `n! = n * (n-1) * (n-2) * ... * 1`
  * 단, `0!`은 `1`로 정의합니다.

**시작 코드:**

```python
def factorial(n):
  # 여기에 코드를 작성하세요
  pass

# 테스트
print(f"5! = {factorial(5)}") # 예상 출력: 120
print(f"0! = {factorial(0)}") # 예상 출력: 1
```